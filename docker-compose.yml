services:
  web:
    image: internconnect-web:local
    container_name: internconnect-web
    build: .
    ports:
      - "${PORT:-8087}:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
